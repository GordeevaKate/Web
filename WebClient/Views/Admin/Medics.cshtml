
@{
    ViewData["Title"] = "Медперсонал";
}
<div class="text-center text-info">
    <h1>Медперсонал</h1>
</div>
<hr />
<p> <strong> <a class="btn btn-info btn-sm" asp-area="" asp-controller="Admin" asp-action="AddMedics">Оформить</a></strong></p>
<hr />
@using (Html.BeginForm())
{
    <table class="table">
        <tr>
            <td>Логин</td>
            <td>Пaроль</td>
        </tr>
        @foreach (var client in ViewBag.Doctors)
        {
            <tr>
                <td>@client.Login</td>
                <td>@client.Password</td>
            </tr>

        }
    </table>
}